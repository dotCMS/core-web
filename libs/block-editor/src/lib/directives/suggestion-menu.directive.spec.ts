import { SuggestionMenuDirective } from './suggestion-menu.directive';

describe('SuggestionMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestionMenuDirective();
    expect(directive).toBeTruthy();
  });
});
